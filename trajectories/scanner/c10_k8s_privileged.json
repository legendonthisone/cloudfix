{
  "system": "scanner",
  "case_id": "c10_k8s_privileged",
  "case_title": "Log shipper deployed with host privilege in the production namespace",
  "model": null,
  "plan_file": "data/plans/c10_k8s_privileged.json",
  "ground_truth": {
    "expected_verdict": "DO NOT APPLY",
    "rules": [
      "D5"
    ],
    "why": "privileged, host_pid, host_network and allow_privilege_escalation are all true in the production namespace. D5. One compromised image becomes a compromised node, and the node runs everything else.",
    "difficulty": "medium"
  },
  "how_to_read": "Steps run in order. kind=tool is deterministic code, kind=model is a call to the language model, kind=skipped is a step this rung deliberately removed. Every model step carries the exact system_prompt and user_prompt it was sent, so the run can be followed from the instructions through to the verdict. A verify step's output.defects is the feedback handed back to the model, and the repair step after it shows what it did with that feedback.",
  "steps": [
    {
      "step": 1,
      "name": "scan",
      "kind": "tool",
      "tool": "checks.run_all",
      "output": {
        "finding_count": 4,
        "findings": [
          {
            "check_id": "K8S_PRIVILEGED_WORKLOAD",
            "title": "Container workload asks for host level privilege",
            "severity": "critical",
            "resource_address": "kubernetes_deployment.log_shipper",
            "resource_type": "kubernetes_deployment",
            "action": "create",
            "environment": "unknown",
            "detail": "host_pid is True. A container with this setting can reach past its own boundary onto the node it runs on, which means a single compromised image becomes a compromised host.",
            "introduced_by_change": true,
            "evidence": [
              {
                "pointer": "resource_changes[0].change.after.spec[0].template[0].spec[0].host_pid",
                "value": true,
                "note": "privilege flag on the workload"
              }
            ]
          },
          {
            "check_id": "K8S_PRIVILEGED_WORKLOAD",
            "title": "Container workload asks for host level privilege",
            "severity": "critical",
            "resource_address": "kubernetes_deployment.log_shipper",
            "resource_type": "kubernetes_deployment",
            "action": "create",
            "environment": "unknown",
            "detail": "privileged is True. A container with this setting can reach past its own boundary onto the node it runs on, which means a single compromised image becomes a compromised host.",
            "introduced_by_change": true,
            "evidence": [
              {
                "pointer": "resource_changes[0].change.after.spec[0].template[0].spec[0].container[0].security_context[0].privileged",
                "value": true,
                "note": "privilege flag on the workload"
              }
            ]
          },
          {
            "check_id": "K8S_PRIVILEGED_WORKLOAD",
            "title": "Container workload asks for host level privilege",
            "severity": "high",
            "resource_address": "kubernetes_deployment.log_shipper",
            "resource_type": "kubernetes_deployment",
            "action": "create",
            "environment": "unknown",
            "detail": "host_network is True. A container with this setting can reach past its own boundary onto the node it runs on, which means a single compromised image becomes a compromised host.",
            "introduced_by_change": true,
            "evidence": [
              {
                "pointer": "resource_changes[0].change.after.spec[0].template[0].spec[0].host_network",
                "value": true,
                "note": "privilege flag on the workload"
              }
            ]
          },
          {
            "check_id": "K8S_PRIVILEGED_WORKLOAD",
            "title": "Container workload asks for host level privilege",
            "severity": "high",
            "resource_address": "kubernetes_deployment.log_shipper",
            "resource_type": "kubernetes_deployment",
            "action": "create",
            "environment": "unknown",
            "detail": "allow_privilege_escalation is True. A container with this setting can reach past its own boundary onto the node it runs on, which means a single compromised image becomes a compromised host.",
            "introduced_by_change": true,
            "evidence": [
              {
                "pointer": "resource_changes[0].change.after.spec[0].template[0].spec[0].container[0].security_context[0].allow_privilege_escalation",
                "value": true,
                "note": "privilege flag on the workload"
              }
            ]
          }
        ]
      }
    },
    {
      "step": 2,
      "name": "gate",
      "kind": "tool",
      "tool": "scanner.GATE",
      "input": {
        "severities": [
          "critical",
          "critical",
          "high",
          "high"
        ]
      },
      "output": {
        "verdict": "DO NOT APPLY"
      }
    }
  ],
  "repairs": 0,
  "error": null,
  "final_decision": {
    "verdict": "DO NOT APPLY",
    "summary": "4 findings from the security checks. The gate blocks on high or above.",
    "rules": [
      "severity gate"
    ],
    "reasons": [
      {
        "claim": "K8S_PRIVILEGED_WORKLOAD on kubernetes_deployment.log_shipper: Container workload asks for host level privilege",
        "evidence_pointer": "resource_changes[0].change.after.spec[0].template[0].spec[0].host_pid",
        "expected_value": "true",
        "rule": "severity gate",
        "supported": null,
        "support_note": ""
      },
      {
        "claim": "K8S_PRIVILEGED_WORKLOAD on kubernetes_deployment.log_shipper: Container workload asks for host level privilege",
        "evidence_pointer": "resource_changes[0].change.after.spec[0].template[0].spec[0].container[0].security_context[0].privileged",
        "expected_value": "true",
        "rule": "severity gate",
        "supported": null,
        "support_note": ""
      },
      {
        "claim": "K8S_PRIVILEGED_WORKLOAD on kubernetes_deployment.log_shipper: Container workload asks for host level privilege",
        "evidence_pointer": "resource_changes[0].change.after.spec[0].template[0].spec[0].host_network",
        "expected_value": "true",
        "rule": "severity gate",
        "supported": null,
        "support_note": ""
      },
      {
        "claim": "K8S_PRIVILEGED_WORKLOAD on kubernetes_deployment.log_shipper: Container workload asks for host level privilege",
        "evidence_pointer": "resource_changes[0].change.after.spec[0].template[0].spec[0].container[0].security_context[0].allow_privilege_escalation",
        "expected_value": "true",
        "rule": "severity gate",
        "supported": null,
        "support_note": ""
      }
    ],
    "dismissed": [],
    "confirmations": [],
    "unsupported_reason_count": 0
  },
  "human_checkpoint": "CloudFix never applies anything. It reads a plan and recommends. Applying the change, or refusing to, is the human's decision and always happens outside this tool.",
  "score": {
    "case_id": "c10_k8s_privileged",
    "difficulty": "medium",
    "expected_verdict": "DO NOT APPLY",
    "found_verdict": "DO NOT APPLY",
    "verdict_correct": true,
    "direction": "exact",
    "dangerous_miss": false,
    "over_block": false,
    "caught_critical": true,
    "reasons_total": 4,
    "reasons_with_pointer": 4,
    "reasons_unsupported": 0,
    "dismissed_total": 0,
    "repairs": 0,
    "error": null
  }
}